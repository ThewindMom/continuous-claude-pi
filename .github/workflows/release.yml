name: Release

on:
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder release step
        run: |
          echo "Create tag/release workflow for ThewindMom/continuous-claude-pi when this package is moved to its own repository."
